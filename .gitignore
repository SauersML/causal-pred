# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Virtualenv
.venv/
venv/

# uv / lockfile
# (we DO commit uv.lock for reproducibility)

# Pipeline outputs (regenerated by `uv run python -m causal_pred.pipeline`)
outputs/

# AoU real-data cache (everything under /data/ except the small MR cache).
/data/*
!/data/mr_cache/
/genomes/

# Paper build artefacts
paper/_build/
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.pdf
paper/*.synctex.gz
paper/figures/*
!paper/figures/crosscoder_10_participant_umap.png

# Editor / OS
.DS_Store
.idea/
.vscode/

.claude/
